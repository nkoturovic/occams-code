# Project-local config (per-project preset + model overrides)
.opencode/

# Secrets and auth
auth.json
budget.json
mcp-oauth.json

# Backups and logs
backups/
logs/

# Node (oh-my-opencode-slim dependency)
node_modules/
bun.lock
package-lock.json

# Python cache
__pycache__/
*.pyc
.ruff_cache/

# OS files
.DS_Store
Thumbs.db

# Obsidian volatile state (wiki has its own .gitignore too)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.obsidian/graph.json
